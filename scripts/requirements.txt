# Whisper Transcription Dependencies
# Install with: pip install -r requirements.txt

# Faster-Whisper (recommended, supports VAD filter)
# https://github.com/SYSTRAN/faster-whisper
faster-whisper>=1.0.0

# OpenAI Whisper (original implementation)
# https://github.com/openai/whisper
# Note: Install one of the following:
# - openai-whisper (official package)
# - whisper (legacy name)
openai-whisper>=20231117

# Optional: PyTorch for GPU support
# Install separately based on your CUDA version:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# or for CPU only:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

